/**
 * Provides authenticated user info
 * @returns
 */
declare const useAuth: () => {
    user?: import("../../services/bridge-service").UserInfo | undefined;
    ready: boolean;
};
export default useAuth;
