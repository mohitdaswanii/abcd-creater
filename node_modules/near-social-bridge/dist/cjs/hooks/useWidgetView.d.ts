/**
 * Provides access to methods and props which can affect the Widget View
 */
declare const useWidgetView: () => {
    /**
     * Update the View height
     * @param height Height
     */
    setHeight: (height: number) => Promise<{}>;
};
export default useWidgetView;
